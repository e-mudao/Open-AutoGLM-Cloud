# --- Core Dependencies ---
# 用于连接智谱 GLM-4.6v (OpenAI 兼容接口)
openai>=1.0.0
httpx

# --- Image Processing ---
# 用于截图处理、压缩和格式转换
Pillow>=10.0.0

# --- Utilities ---
# 基础工具库 (保留原项目中可能用到的轻量级依赖)
numpy
tqdm
requests
typing_extensions
colorama
